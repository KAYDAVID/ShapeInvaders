using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScreenSize : MonoBehaviour {

	// Use this for initialization
	void Awake () {
        //Screen.SetResolution(300, 700, false);
	}

	// Update is called once per frame
	void Update () {
		
	}
}
